extends layout

block append head
	link(rel='stylesheet' href="/css/hackathon.css")

block content 

	.hackathon
		header#mu-hero
			.mu-hero-overlay
				.container
					.mu-hero-area
						.mu-hero-featured-area
							.mu-logo-area
								img.mu-logo-img
								a.mu-logo(href='#') 
							.mu-hero-featured-content
		.bg-light.text-dark
			.container
				.about.center
					h2 About The Hackathon

		.container
			.sponsors.center
				h2 Sponsors
		
		.bg-light.text-dark
			.container
				.row
					a(href="/vis") View All in Visual
				.hackers.center
					h2 Attending Hackers
					.row.attending-hackers
						
	script.
		var hackathon = !{JSON.stringify(foundHackathon)};
		var matches = !{JSON.stringify(data)};

	script(src="/js/hackathon.js")


		
	